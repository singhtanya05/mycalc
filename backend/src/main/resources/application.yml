server:
  port: 8081

spring:
  application:
    name: calc-backend
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB

logging:
  level:
    root: INFO
    com.portfolio.calc: DEBUG
